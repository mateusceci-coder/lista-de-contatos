export interface contactParams {
    id: number
    name: string
    phone: string
    address: string
    email: string
}

export interface getContactParams {
    id: number
    name: string
    phone: string
    address: string
    email: string
}