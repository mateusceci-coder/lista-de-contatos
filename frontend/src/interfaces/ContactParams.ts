export interface contactParams {
    name: string
    phone: string
    address: string
    email: string
}

export interface getContactParams {
    id: number
    name: string
    phone: string
    address: string
    email: string
}